<div class="relative overflow-x-auto bg-white">
  <table class="min-w-full divide-y divide-slate-200 text-sm text-left text-slate-600">
    <thead class="bg-slate-50 text-xs uppercase tracking-[0.2em] text-slate-500">
      <tr>
        <th scope="col" class="px-5 py-3">Email</th>
        <th scope="col" class="px-5 py-3">Action</th>
      </tr>
    </thead>
    <tbody class="divide-y divide-slate-100 bg-white">
      <% @subscribe.each do |subscribe| %>
        <tr class="hover:bg-slate-50">
          <th scope="row" class="whitespace-nowrap px-5 py-4 font-medium text-slate-900">
            <%= subscribe.id %> | <%= subscribe.email %>
          </th>
          <td class="px-5 py-4 text-slate-500">
            <span class="inline-flex items-center rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-700">Subscribed</span>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
